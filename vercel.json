{
  "version": 2,
  "buildCommand": "cd apps/web && bun run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "bun install --frozen-lockfile",
  "env": {
    "NEXT_PUBLIC_API_URL": "https://server-production-3365.up.railway.app"
  }
}
